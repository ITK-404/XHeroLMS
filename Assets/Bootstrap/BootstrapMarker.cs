using UnityEngine;

[CreateAssetMenu(
    fileName = "BootstrapMarker",
    menuName = "Bootstrap/Marker",
    order = 1
)]
public class BootstrapMarker : ScriptableObject
{
    // marker rỗng, không cần code
}
